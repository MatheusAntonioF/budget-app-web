import styled from 'styled-components';

import { Core } from '../styles';

export const DatepickerCore = styled(Core)`
  input {
    width: 100%;
    height: 100%;
    outline: 0;
    border: 0;

    padding-left: 10px;
  }
`;
